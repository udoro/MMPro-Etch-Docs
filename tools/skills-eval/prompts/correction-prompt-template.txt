Your skills documentation is at:
__SKILLS_PATH__

That file, its reference companion in the same folder, and the `components/` folder it points at are the only documentation you may read. Ignore any other skills or README files in this repository.

Keep temp scripts in a subdirectory you create, and delete it when you are done. Do not delete files from shared directories.

The Etch AI Connector is running and a builder tab is connected: etchtuts.designwithcracka.com

---

I want the Search dropdown to open on hover instead of click.

---

At the end, add a section headed `## Run metrics` with exactly these lines:
- `Applied:` what you changed.
- `Connector eval calls:` count.
- `Tool calls:` count.
- `Skills file: lines read:` roughly how many lines of the skills file and its companion you read, and whether you read the skills file in full.
- `Schema dumps:` how many full component property schemas you dumped.
- `Verification level reached:` `configuration` or `rendered`, and one sentence on how.
Report honestly, including anything that went badly.
